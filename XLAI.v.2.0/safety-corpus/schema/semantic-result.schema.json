{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://xlai.local/safety-corpus/schema/semantic-result.schema.json",
  "title": "XLAI Semantic Safety Result",
  "description": "An internal-only future semantic safety classifier result. It contains classification evidence for shadow-mode comparison and evaluation, not generated text or enforcement policy.",
  "type": "object",
  "additionalProperties": false,
  "required": ["category", "level", "confidence", "semanticSignals", "reasoningSummary"],
  "properties": {
    "category": {
      "type": "string",
      "enum": ["none", "emotional_distress", "conflict_crisis", "unsafe_relationship_dynamics", "abuse_or_coercion", "stalking_or_tracking", "threats_or_intimidation", "coercive_control_or_isolation", "home_danger", "self_harm_or_suicide", "violence_risk", "immediate_danger"],
      "description": "Existing Safety Knowledge Base category key only."
    },
    "level": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5,
      "description": "Existing XLAI safety level from 0 through 5."
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1
    },
    "semanticSignals": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Concise semantic evidence labels; these do not replace deterministic matched signals."
    },
    "reasoningSummary": {
      "type": "string",
      "minLength": 1,
      "description": "Concise non-sensitive classifier explanation for debugging and evaluation, not hidden chain-of-thought."
    }
  }
}
